import { createIcon } from "@md3-ui/icon"
import * as React from "react"
import { Path } from "react-native-svg"

export const Square = createIcon({
  path: <Path d="M3 3h18v18H3z" />,
  displayName: "Square",
})
