import { createIcon } from "@md3-ui/icon"
import * as React from "react"
import { Path } from "react-native-svg"

export const Maximize = createIcon({
  path: <Path d="M3 3h18v2H3z" />,
  displayName: "Maximize",
})
