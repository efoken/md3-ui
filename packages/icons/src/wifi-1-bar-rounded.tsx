import { createIcon } from "@md3-ui/icon"
import * as React from "react"
import { Circle } from "react-native-svg"

export const Wifi1BarRounded = createIcon({
  path: <Circle cx={12} cy={18} r={2} />,
  displayName: "Wifi1BarRounded",
})
