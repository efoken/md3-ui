import { createIcon } from "@md3-ui/icon"
import * as React from "react"
import { Circle } from "react-native-svg"

export const Brightness1Sharp = createIcon({
  path: <Circle cx={12} cy={12} r={10} />,
  displayName: "Brightness1Sharp",
})
