import { Layout } from "../../src/layouts/layout"

export const meta = {
  title: "Quick start",
}

## Install

Install MD3-UI using [npm and node](https://nodejs.org/).

```bash
npm install --save @md3-ui/core
```

export default ({ children }) => <Layout meta={meta}>{children}</Layout>
